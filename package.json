{
  "name": "url_shortener",
  "version": "1.0.0",
  "description": "A REST API to make short url for large ones, so tht we can remember",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sumit Singh",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
